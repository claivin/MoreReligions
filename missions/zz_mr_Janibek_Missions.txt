janibek_missions_first = {
	slot = 3
	generic = no
	ai = yes
	has_country_shield = yes
	potential = {
		tag = NOG
	}
	jb_end_war_nogay = {
		icon = "mission_mnd_siam_royal_duel"
		required_missions = {}
		position = 1
		trigger = {
		OR = {
		    war_score_against = {
	          who = SHY
	          value = 75
			}
			NOT = { exists = SHY }			
			}
		    controls = 1970
			controls = 476
		}
		effect = {
		if = {
			limit = {exists = SHY}
			        inherit = SHY}
					
			change_tag = KZH
		    on_change_tag_effect = yes
			set_capital = 1970
			
			hidden_effect = {
			ROOT = { every_owned_province = {remove_core = SHY}
			       }
			}
		}
	}
}
