kerei_missions_first = {
	slot = 3
	generic = no
	ai = yes
	has_country_shield = yes
	potential = {
		tag = SHY
	}
	kei_end_war_nogay = {
		icon = "mission_mnd_siam_royal_duel"
		required_missions = {}
		position = 1
		trigger = {
		OR = {
		    war_score_against = {
	          who = NOG
	          value = 75
			}
			NOT = { exists = NOG }			
			}
		    controls = 465
		}
		effect = {
		if = {
			limit = {exists = NOG}
			        inherit = NOG}

			change_tag = KZH
		    on_change_tag_effect = yes
			
		    hidden_effect = {
			ROOT = { every_owned_province = {remove_core = NOG}
			       }
			}
		}
	}
}
