estate_sarraceni = { # Christian Dhimmi, from *sarracenus* to denote muslim peasants in Outremer
	icon = 6
	other_religion_group_only = yes

	province_independence_weight = {
		factor = 1
		modifier = {
			factor = 0.1
			religion_group = christian
		}
		# modifier = {
		# 	factor = 1.5
		# 	has_province_modifier = repression_of_influential_sarraceni
		# }
	}

	# If true, country will get estate
	trigger = {
		# has_dlc = "The Cossacks"
		has_country_flag = enables_estate_sarraceni_flag
		AND = {
			NOT = { is_revolutionary_republic_trigger = yes }
			NOT = { has_government_attribute = is_pirate_republic_reform }
			is_nomad = no
			is_colonial_nation = no
			NOT = { has_reform = celestial_empire }
			religion_group = christian
			NOT = { has_reform = revolutionary_empire_reform }
		}
	}

	# These scale with loyalty & power
	country_modifier_happy = {
		technology_cost = -0.1
		tolerance_heathen = 2
	}
	country_modifier_neutral = {
		tolerance_heathen = 2
	}
	country_modifier_angry = {
		technology_cost = 0.1
		tolerance_heathen = -1
		global_unrest = 2
	}
	land_ownership_modifier = {
		sarraceni_loyalty_modifier = 0.2
	}


	# Influence modifiers
	base_influence = 0
	influence_modifier = {
		desc = EST_VAL_RELIGIOUS_MINORITY
		trigger = {
			any_owned_province = {
				NOT = { religion_group = christian }
			}
		}
		influence = 10
	}
	influence_modifier = {
		desc = EST_VAL_RULED_BY_RELIGIOUS_MINORITY
		trigger = {
			NOT = { dominant_religion = ROOT }
			any_owned_province = {
				NOT = { religion_group = christian }
			}
		}
		influence = 20
	}
	influence_modifier = {
		desc = EST_VAL_OTHER_ESTATE_IN_POWER
		trigger = {
			OR = {
				has_disaster = estate_nobility_disaster
				has_disaster = estate_church_disaster
				has_disaster = estate_burghers_disaster
				has_disaster = estate_brahmins_disaster
				has_disaster = estate_vaisyas_disaster
				has_disaster = estate_jains_disaster
			}
		}
		influence = -40
	}

	influence_from_dev_modifier = 0.5

	#Loyalty Modifiers
	loyalty_modifier = {
		desc = EST_VAL_OTHER_ESTATE_IN_POWER_LOY
		trigger = {
			OR = {
				has_disaster = estate_nobility_disaster
				has_disaster = estate_church_disaster
				has_disaster = estate_burghers_disaster
				has_disaster = estate_brahmins_disaster
				has_disaster = estate_vaisyas_disaster
				has_disaster = estate_jains_disaster
			}
		}
		loyalty = -20
	}
	loyalty_modifier = {
		desc = EST_VAL_HEATHEN_PEACE_LOYALTY
		trigger = {
			NOT = {
				any_known_country = {
					war_with = ROOT
					NOT = { religion_group = ROOT }
				}
			}
		}
		loyalty = 10
	}

	privileges = {
		# estate_sarraceni_land_rights
		# estate_sarraceni_lighter_sarraceni_taxes
		estate_sarraceni_guaranteed_autonomy
		# estate_sarraceni_sarraceni_nobles
		# estate_sarraceni_manpower
		# estate_sarraceni_tolerance
		# estate_sarraceni_administrative_privileges
		# estate_sarraceni_millet_system
		# estate_sarraceni_guarantee_of_traditions
		# estate_sarraceni_grant_liberties
		# estate_sarraceni_development_of_communities
		# estate_sarraceni_grant_trading_privileges
		# estate_sarraceni_sarraceni_education
		# estate_sarraceni_promote_sarraceni_nobility
		# #estate_sarraceni_military_service
		# estate_sarraceni_protected_communities
		# estate_sarraceni_contribution_demand
	}

	agendas = {
		# estate_sarraceni_develop_x
		# estate_sarraceni_build_temple_in_y
		# estate_sarraceni_build_university_in_y
		# estate_sarraceni_stop_converting_province_x
		# estate_sarraceni_increase_autonomy_in_province_x
		# estate_sarraceni_three_privileges
		# estate_sarraceni_relations_with_X
		# estate_sarraceni_hire_advisor
		# estate_sarraceni_fire_advisor
	}

	color = { 100 125 150 }
}
